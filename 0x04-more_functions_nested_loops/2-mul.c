#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: character to be checked
 * @b: character to be checked
 * Return: 0 on success
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
